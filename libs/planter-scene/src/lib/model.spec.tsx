import { describe, it, expect } from 'vitest';
import { PlanterModel } from './model';

describe('PlanterModel', () => {
  it('is a component', () => {
    expect(typeof PlanterModel).toBe('function');
  });
});
