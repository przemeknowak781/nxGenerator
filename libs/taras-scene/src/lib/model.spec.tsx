import { describe, it, expect } from 'vitest';
import { TarasModel } from './model';

describe('TarasModel', () => {
  it('is a component', () => {
    expect(typeof TarasModel).toBe('function');
  });
});
